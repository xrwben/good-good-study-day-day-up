const name = 'liben'

const getName = () => {
	console.log('name is liben !!!')
}

const flag = ['a', 'b'].includes('a') ? 1 : 2

const set = new Set([1, 2, 3])

// const promise = new Promise()

const flags = ['a', 'b'].includes('a') ? 1 : 2

// 注释注释

export {
	name,
	getName,
	flag,
	set,
	// promise,
	flags
}